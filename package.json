{
  "name": "lineshow",
  "version": "0.0.7",
  "description": "",
  "main": "index.js",
  "bin": {
    "lineshow": "bin/index.js"
  },
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx esbuild ./src/*.js `find ./src/components/*js` --loader:.js=jsx --outdir=bin --platform=node --format=esm"
  },
  "author": "= <=>",
  "license": "MIT",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@babel/parser": "^7.21.4",
    "@babel/traverse": "^7.21.4",
    "esbuild": "^0.17.11",
    "mocha": "^10.2.0"
  }
}
