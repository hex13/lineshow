{
  "name": "lineshow",
  "version": "0.0.7",
  "description": "",
  "main": "index.js",
  "bin": {
    "lineshow": "bin/index.js"
  },
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx esbuild ./src/index.js `find ./src/components/*js` --loader:.js=jsx --outdir=bin --platform=node --format=esm"
  },
  "author": "= <=>",
  "license": "MIT",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.17.11"
  }
}
